using dotless.Core.Input;

namespace dotless.Bundling
{
    class BundlingPathResolver : IPathResolver
    {
        public string GetFullPath(string path)
        {
            return path;
        }
    }
}
